# 变道碰撞问题解决计划

## 问题描述

当前系统在车辆变道时存在安全隐患：即使对方车辆不在当前行进路线规划上，但在我们即将变入的车道上行驶时，系统仍会执行变道操作，导致可能发生碰撞。具体表现为：

1. 系统没有检测目标车道上是否有其他车辆
2. 变道决策只考虑了道路几何形状和距离，没有考虑交通安全因素
3. 缺乏对变道目标车道交通状况的评估机制

## 代码分析

通过分析现有代码，发现以下关键问题：

1. 在 `Map/route/planning.py` 和 `Map/route/driving.py` 中的变道逻辑主要基于道路几何和用户输入（转向灯），没有考虑其他车辆的存在
2. `AdaptiveCruiseControl` 插件中的 `get_vehicle_in_front` 函数只检测前方车辆，没有检测侧方车辆
3. `Traffic` 模块提供了车辆信息，但这些信息没有被用于变道决策
4. 变道操作 `RouteSection.lane_index` 的设置没有安全检查机制

## 解决方案

### 1. 添加变道安全检测功能

在 `Map/route/planning.py` 的 `CheckForLaneChange` 函数中添加对目标车道交通状况的检测：

```python
def is_lane_change_safe(current_lane_index, target_lane_index):
    # 获取所有车辆
    vehicles = data.plugin.modules.Traffic.run()
    if vehicles is None or vehicles == []:
        return True  # 没有车辆，变道安全
    
    # 计算目标车道的边界
    current_lane = data.route_plan[0].items[0].item.lanes[current_lane_index]
    target_lane = data.route_plan[0].items[0].item.lanes[target_lane_index]
    
    # 获取当前车辆位置和方向
    truck_x = data.truck_x
    truck_z = data.truck_z
    truck_rotation = data.truck_rotation
    truck_speed = data.truck_speed
    
    # 检查每个车辆是否在目标车道上且可能造成碰撞
    for vehicle in vehicles:
        # 计算车辆是否在目标车道上
        vehicle_x = vehicle.position.x
        vehicle_z = vehicle.position.z
        
        # 计算车辆与我们的相对位置
        distance = math_helpers.DistanceBetweenPoints((vehicle_x, vehicle_z), (truck_x, truck_z))
        
        # 如果车辆在合理的检测范围内（前后30米）
        if distance < 30:
            # 判断车辆是否在目标车道上
            is_in_target_lane = is_vehicle_in_lane(vehicle, target_lane)
            
            if is_in_target_lane:
                # 计算相对速度和安全距离
                relative_speed = vehicle.speed - truck_speed
                safe_distance = max(10, abs(relative_speed) * 2)  # 简单安全距离计算
                
                # 如果距离小于安全距离，变道不安全
                if distance < safe_distance:
                    return False
    
    return True
```

### 2. 在变道决策中集成安全检测

修改 `Map/route/planning.py` 中的变道决策逻辑：

```python
# 在决定变道前检查安全性
if target != current_index:
    # 检查变道安全性
    if not is_lane_change_safe(current_index, target):
        data.plugin.globals.tags.lane_change_status = "unsafe"
        data.plugin.state.text = "变道不安全，请等待。"
        if time.time() - data.last_sound_played > data.sound_play_interval:
            sounds.Play("warning")  # 播放警告音
            data.last_sound_played = time.time()
        return
    
    planned = current.get_planned_lane_change_distance()
    left = current.distance_left()
    # 其余代码保持不变...
```

### 3. 添加车辆在车道上的检测函数

在 `Map/utils/road_helpers.py` 中添加新的辅助函数：

```python
def is_vehicle_in_lane(vehicle, lane):
    """判断车辆是否在指定车道上"""
    # 获取车辆的四个角点
    corners = vehicle.get_corners()
    
    # 获取车道的边界点
    lane_points = lane.points
    if len(lane_points) < 2:
        return False
    
    # 简化：检查车辆中心点是否在车道附近
    vehicle_center = (vehicle.position.x, vehicle.position.z)
    
    # 找到最近的车道点
    min_distance = float('inf')
    for i in range(len(lane_points) - 1):
        p1 = (lane_points[i].x, lane_points[i].z)
        p2 = (lane_points[i+1].x, lane_points[i+1].z)
        
        # 计算点到线段的距离
        distance = point_to_line_distance(vehicle_center, p1, p2)
        min_distance = min(min_distance, distance)
    
    # 如果距离小于车道宽度的一半，认为车辆在车道上
    return min_distance < (lane.width / 2)

def point_to_line_distance(point, line_start, line_end):
    """计算点到线段的距离"""
    x, y = point
    x1, y1 = line_start
    x2, y2 = line_end
    
    # 线段长度的平方
    l2 = (x2 - x1)**2 + (y2 - y1)**2
    
    # 如果线段长度为0，直接返回点到起点的距离
    if l2 == 0:
        return math.sqrt((x - x1)**2 + (y - y1)**2)
    
    # 计算投影比例 t
    t = max(0, min(1, ((x - x1) * (x2 - x1) + (y - y1) * (y2 - y1)) / l2))
    
    # 计算投影点
    projection_x = x1 + t * (x2 - x1)
    projection_y = y1 + t * (y2 - y1)
    
    # 返回点到投影点的距离
    return math.sqrt((x - projection_x)**2 + (y - projection_y)**2)
```

### 4. 添加变道安全状态显示

在 HUD 插件中添加变道安全状态的显示：

```python
# 在 HUD/main.py 的 LaneChangeUpdater 方法中添加
if status == "unsafe":
    self.lane_change_data = []
    # 添加警告显示
    offset_x, offset_y, offset_z = self.get_offsets()
    anchor = Coordinate(0 + offset_x, -2 + offset_y, -10 + offset_z, relative=True, rotation_relative=True)
    
    # 创建警告图标和文本
    warning_icon = Text("⚠", anchor, size=2, color=(255, 0, 0, 255))
    warning_text = Text("变道不安全", anchor.offset(y=2), size=1, color=(255, 0, 0, 255))
    
    self.lane_change_data = [warning_icon, warning_text]
    time.sleep(1/2)  # 2fps
    continue
```

## 实施步骤

1. 实现 `is_vehicle_in_lane` 和 `point_to_line_distance` 辅助函数
2. 实现 `is_lane_change_safe` 变道安全检测函数
3. 修改 `CheckForLaneChange` 函数，集成安全检测
4. 更新 HUD 显示，添加变道安全状态提示
5. 测试变道安全检测功能
6. 优化参数，提高检测准确性

## 预期效果

1. 系统能够检测目标车道上的车辆，并评估变道安全性
2. 当变道不安全时，系统会提供视觉和声音警告
3. 只有在确认安全的情况下才执行变道操作
4. 减少变道过程中的碰撞风险

## 后续优化方向

1. 改进车辆轨迹预测，考虑车辆未来可能的位置
2. 添加多级安全评估，区分"不安全"和"需要注意"的情况
3. 实现自适应安全距离计算，根据车速和道路条件调整
4. 添加变道辅助线，直观显示安全和不安全区域
5. 考虑添加车道变换请求确认机制，增加用户控制